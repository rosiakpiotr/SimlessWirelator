# Wireless Reachability Simulator

Works best when ran from root of this repo.
Make sure to install all packages *with versions specified* as specified in `requirements.txt`

**Entry file**: `main.py`